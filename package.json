{
  "name": "anton-okx-signer",
  "version": "1.1.0",
  "private": true,
  "description": "Guarded OKX spot execution with independent market-only signals and existing position monitoring",
  "main": "market-runner.js",
  "scripts": {
    "start": "npm run check && npm test && node market-runner.js",
    "check": "node --check market-runner.js && node --check market-strategy.js && node --check risk-monitor.js && node --check safety-gateway.js && node --check server.js",
    "test": "node --test risk-monitor.test.js market-strategy.test.js"
  },
  "engines": {"node": ">=20"}
}