{
  "name": "anton-okx-signer",
  "version": "1.1.1",
  "private": true,
  "description": "Guarded OKX spot execution with independent market-only signals and authenticated dashboard",
  "main": "dashboard-launcher.js",
  "scripts": {
    "start": "npm run check && npm test && node dashboard-launcher.js",
    "check": "node --check dashboard-launcher.js && node --check market-runner.js && node --check market-strategy.js && node --check risk-monitor.js && node --check safety-gateway.js && node --check server.js",
    "test": "node --test dashboard-launcher.test.js risk-monitor.test.js market-strategy.test.js"
  },
  "engines": {"node": ">=20"}
}
