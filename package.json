{
  "name": "anton-arbitrage-paper",
  "version": "0.2.0",
  "private": true,
  "description": "Isolated paper-only read-only public market arbitrage monitor; no exchange trading",
  "main": "arbitrage/server.js",
  "scripts": {
    "start": "npm run check && npm test && node arbitrage/server.js",
    "check": "node --check arbitrage/scanner.js && node --check arbitrage/live-feed.js && node --check arbitrage/server.js && node --check arbitrage/live-feed.test.js",
    "test": "node --test arbitrage/scanner.test.js arbitrage/live-feed.test.js"
  },
  "engines": {"node": ">=20"}
}
