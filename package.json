{
  "name": "anton-okx-signer",
  "version": "1.0.2",
  "private": true,
  "description": "Guarded OKX spot execution and independent position monitoring for ANTON Signal",
  "main": "risk-monitor.js",
  "scripts": {
    "start": "node risk-monitor.js",
    "check": "node --check risk-monitor.js && node --check safety-gateway.js && node --check server.js",
    "test": "node --test risk-monitor.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
