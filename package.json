{
  "name": "anton-arbitrage-paper",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated paper-only arbitrage evaluator; no exchange trading",
  "main": "arbitrage/server.js",
  "scripts": {
    "start": "node arbitrage/server.js",
    "check": "node --check arbitrage/scanner.js && node --check arbitrage/server.js",
    "test": "node --test arbitrage/scanner.test.js"
  },
  "engines": {"node": ">=20"}
}
