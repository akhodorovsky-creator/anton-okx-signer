Release gates: no real trading smoke tests. Verify full code syntax and Node regression suite, verify compatibility patch against the actual source, verify Railway deployment health, confirm mode and limit logs, then inspect OKX balances and order history read-only. The ETH exit issue cannot be called resolved until exchange min size and actual free balance permit a sale or a manually reconciled resolution is documented.
