Issue: old independent BTC monitor called /auto using 3% profit exit while multi-pair coordinator used 5%, leading to conflicting exits. Fix branch disables legacy auto when multi mode is active, rejects public /auto, retains original multi-pair TP 5% SL 2%, logs unsellable ETH conditions, and adds tests. This note does not assert real deployment or complete remediation of ETH min-size limitations.
